# 0.5.4

## Documentation

- Added four Mermaid diagrams to README:
  - **Visual Overview** — top-down containment hierarchy of ArgumentEngine, showing owned entities (PremiseEngine, Variables, Roles) and injected libraries (ClaimLibrary, SourceLibrary, ClaimSourceLibrary)
  - **Expression Tree** — concrete tree for `¬(P ∧ R) → (Q ∨ S)` showing operator, variable, and formula node types with arity annotations
  - **Argument Composition** — premises with role annotations (conclusion, supporting, constraint), shared variables, and role derivation rules
  - **Evaluation Flow** — left-to-right pipeline from input through validation, constraint checking, supporting premise evaluation, conclusion evaluation, and counterexample detection
