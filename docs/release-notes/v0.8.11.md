# Release Notes

## New

- **Formula tree walker** — `PremiseEngine.walkFormulaTree(visitor)` lets you traverse a premise's expression tree with a visitor pattern, producing any output type. Use it to render formulae as React components, build custom AST representations, or implement your own display logic without parsing the `toDisplayString()` output.
