# Changelog — v0.8.8

## IEEE config-driven builder (`eab3b71..a40ba24`)

- **feat(ieee):** Add `TSegmentInstruction` type and `TSegmentTemplate` for declarative citation templates (`eab3b71`)
- **feat(ieee):** Add `buildSegments()` template engine in `segment-builder.ts` with `formatSingleAuthor`, `formatNamesInCitation`, `formatDate`, `IEEE_MONTHS` as canonical exports (`d7dbf55`)
- **feat(ieee):** Port all 33 reference types to segment template configs in `segment-templates.ts` (`820e6d6`)
- **refactor(ieee):** Replace 33 hand-written `*Segments` functions in `formatting.ts` with config-driven `TEMPLATES` dispatch map; formatting.ts reduced from 1168 to 114 lines (`a40ba24`)
- **chore(ieee):** Remove tautological parity tests after switchover (`1f9c823`)

## Extract evaluation from ArgumentEngine (`15c42f8..9cca63c`)

- **feat:** Add `argument-evaluation.ts` module with `TArgumentEvaluationContext`, `TEvaluablePremise` interfaces and stub functions (`15c42f8`)
- **refactor:** Move `propagateOperatorConstraints`, `evaluate`, `checkValidity` (~690 lines) to standalone functions; ArgumentEngine delegates via `asEvaluationContext()` (`9cca63c`)

## Extract validation from ArgumentEngine (`b4a07f2`)

- **refactor:** Move `validate`, `validateAfterPremiseMutation`, `validateEvaluability`, `collectReferencedVariables` (~360 lines) to standalone functions in `argument-validation.ts`; ArgumentEngine delegates via `asValidationContext()` (`b4a07f2`)
- Removed unused imports from ArgumentEngine: `Value`, `CoreArgumentSchema`, `ARG_*` constants, `getOrCreate`, `sortedUnique`, Kleene functions

## Net effect on ArgumentEngine

- `argument-engine.ts`: 2915 → ~1870 lines (~1045 line reduction, 36%)
- New modules: `evaluation/argument-evaluation.ts` (717 lines), `argument-validation.ts` (~350 lines)
