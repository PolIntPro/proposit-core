# v0.6.2

<changes starting-hash="c2b51a0" ending-hash="8c3a4d3">

## Changed

- `src/lib/parsing/prompt-builder.ts` — Added "Sources (External Citations)" section to `CORE_PROMPT` explaining that sources are external citations, not other claims; expanded MiniId Conventions with strict cross-type reference rules (`sourceMiniIds` → `s`-prefixed only, `claimMiniId` → `c`-prefixed only, `conclusionPremiseMiniId` → `p`-prefixed only); added empty-array guidance when no citations exist
- `src/lib/parsing/schemata.ts` — Added `description` annotation to `sourceMiniIds` field in `ParsedClaimSchema` reinforcing that it must contain source references, not claim references

## Internal

- `test/core.test.ts` — Added test verifying the new source/claim disambiguation text is present in the generated prompt

</changes>
